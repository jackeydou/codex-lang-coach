{
  "name": "@language-coach/plugin",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@language-coach/core": "workspace:*"
  }
}
